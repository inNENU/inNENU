<back-button />
<view
  class="header-component {{title.length > 7 ? 'small': ''}}"
  style="padding-top:{{statusBarHeight + 6}}px;"
>
  <text>{{title}}</text>
</view>
<view
  class="header-placeholder"
  style="height: {{height || (statusBarHeight + 60)}}px"
/>
