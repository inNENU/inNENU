module.exports = {
  /** 播放模式 */
  getMode: function (mode) {
    var modeMap = {
      "列表循环": "cycle",
      "单曲循环": "single",
      "顺序播放": "order",
      "随机播放": "random",
    };

    return "./icon/" + modeMap[mode] + "-play.svg";
  },
};
